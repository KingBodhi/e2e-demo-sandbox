# E2E Demo Sandbox

Disposable repo for ORCHA E2E demo agent simulations.

**Do not use for production code.** This repo is used by automated tests to:
- Create branches and commits (simulating dev agent work)
- Open and close PRs (simulating agent PR creation)
- Post review comments (simulating QA watcher verdicts)

Branches and PRs are cleaned up automatically after each demo run.

